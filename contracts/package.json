{
  "name": "agent-did-contracts",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Hardhat workspace for Agent-DID contracts",
  "scripts": {
    "build": "hardhat compile",
    "node:local": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy-agent-registry.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-agent-registry.js --network sepolia",
    "export:abi": "node scripts/export-abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  }
}
